-- 回退 066。整張表拿掉 —— 匯出作業與產出的檔案是衍生資料，
-- 來源（工單、預約、狀態歷程）都還在，重跑一次就有。
--
-- 物件儲存裡的檔案不會被這支刪掉：migration 碰不到 S3。清理由未來的
-- 保存政策決定 —— 與 054 記過的同一個判斷（那個期限是管理者該定義的條件）。
BEGIN;
DROP TABLE IF EXISTS fms.report_exports;
COMMIT;
